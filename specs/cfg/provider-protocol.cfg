SPECIFICATION Spec
CONSTANTS
    IDS = {1, 2, 3}
    DEADLINE = 3
    MAXPARTIAL = 2
    MAXREBUILD = 2
    BACKOFFCAP = 2
\* CorrelationSafety first: it is the invariant the kept mutant must
\* die on, so TLC names it in the shortest violation trace.
INVARIANTS
    CorrelationSafety
    UniqueTerminal
    PartialOrder
    TimeoutLiveness
    RestartFailClosed
    CancelAdvisory
    NoIdReuse
    TypeOK
