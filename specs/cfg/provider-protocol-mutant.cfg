SPECIFICATION Spec
CONSTANTS
    IDS = {1, 2, 3}
    DEADLINE = 3
    MAXPARTIAL = 2
    MAXREBUILD = 2
    BACKOFFCAP = 2
\* Same contract as the base config: the mutant must die on the SAME
\* invariant list, CorrelationSafety first (the gate greps for it).
INVARIANTS
    CorrelationSafety
    UniqueTerminal
    PartialOrder
    TimeoutLiveness
    RestartFailClosed
    CancelAdvisory
    NoIdReuse
    TypeOK
