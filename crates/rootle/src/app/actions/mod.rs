//! Action dispatch by domain (plans/0021 M1): each file owns one
//! arm family of the former app/mod.rs match — a pure move.

pub(super) mod browse;
pub(super) mod lenses;
pub(super) mod lifecycle;
pub(super) mod search;
