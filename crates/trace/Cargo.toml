[package]
name = "rootle-trace"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "rootle's opt-in diagnostic session sink: one private bounded JSONL file with ordered records, explicit failures and completion"

[dependencies]
arc-swap = "1.7"
parking_lot = "0.12"
serde = { version = "1", features = ["derive"] }
serde_json = "1.0.151"
thiserror = "2.0.20"
